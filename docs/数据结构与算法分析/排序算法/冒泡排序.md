```c++
void bubbleSort(int array[], int len)
{
    for(int i = 0; i < len; ++i)
    {
    	for(int j = i+1; j < len; ++j)
        {
            if(array[i] > array[j])
            {
                swap(array[i], array[j]);
            }
		}
    }
}
```

